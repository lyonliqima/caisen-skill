# 七专家分析师 Agent - 可选扩展依赖（体积较大，按需安装）
# 用途: alphaear 系列语义搜索/NLP、量化可视化、行情下载、TradingView 抓取等高级功能
# 注意: torch / transformers / sentence-transformers 体积大（数 GB），按需安装
# 安装: pip install -r requirements-extras.txt

# --- 可视化 / 图像 ---
numpy>=1.26.0
matplotlib>=3.8.0
pyecharts>=2.0.0
Pillow>=10.0.0

# --- 行情数据下载 ---
yfinance>=0.2.30
akshare>=1.10.0

# --- NLP / 语义搜索（alphaear-search / alphaear-sentiment，体积大） ---
scikit-learn>=1.3.0
sentence-transformers>=2.2.0
transformers>=4.36.0
torch>=2.1.0
einops>=0.7.0
huggingface_hub>=0.20.0
rank_bm25>=0.2.2

# --- 爬虫（TradingView 价格回填 / 动态页面） ---
playwright>=1.40.0
tradingview-scraper==0.4.20

# --- Agent / 数据校验框架（alphaear reporter / sentiment 等） ---
agno>=0.1.0
pydantic>=2.5.0

# --- RSS / 进度条 ---
rss-parser>=0.2.7
tqdm>=4.66.0
