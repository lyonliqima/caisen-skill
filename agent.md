# agent.md — 蔡森 skill 代理约定

> 本文件记录 agent（WorkBuddy）在「蔡森破底翻 / 形态学」相关任务中的固定约定，
> 用户希望以后提到相关需求时，agent 直接按此执行，无需再次确认。

## 约定 1：破底翻选股

**触发条件**：用户提到「选破底翻股票」「挑破底翻」「破底翻筛选」「找破底翻」等类似表述。

**执行动作**：直接运行脚本
`./蔡森破底翻量化筛选器.py`

**脚本要点（运行前须知）**：
- 数据源：
  - 上涨股列表：新浪财经列表 API（无需密钥）。
  - K线历史：**多源容错链，东方财富首选**，自动降级。
    - 优先级：**东方财富 push2his（首选）** → 新浪 K线 API → 腾讯财经 fqkline（最终兜底）。
    - 三者均返回统一结构 `list[dict(date,open,close,high,low,volume)]`，detector 无感切换。
    - 东方财富被限流/不可达时，自动落到新浪/腾讯，不会因单接口故障导致整轮失败。
    - ⚠️ 沙箱运行环境对 `push2his.eastmoney.com` 网络层拦截（RemoteDisconnected），
      故在 WorkBuddy 沙箱内跑会直接降级到新浪/腾讯；**用户本机运行东方财富可正常首选**。
- 逻辑（v8 · 收盘价口径 · 全市场宽口径）：默认全市场扫描（**不要求今日大涨**），逐只检测「破底翻 + 守住前低 + 长期历史低位」。
  - 长期低位过滤：历史分位 < 30%（`--bottom-pct` 可调）且距一年高点跌幅 > 40%（`--bottom-drop` 可调）。
  - 两种入选类型（满足其一即可）：
      A. **破底翻（经典）**：60 日窗口内存在前低支撑 → 之后【收盘价】跌破前低（破底）→ 当前【收盘价】已站回前低之上（翻回）→ 破底后未再创更低收盘。
      B. **守住前低（用户口径）**：存在前低支撑且盘中曾测试（最低价触及前低）→ 全程【收盘价】从未有效跌破前低（收盘价没破前低）→ 且已连续 ≥ `--hold-days` 天（默认 3）收盘站上前低之上（几天稳定在前低上面）。
    ※ 两类均以【收盘价】为判定基准（盘中影线破但收盘站回 = 不算破，蔡森铁律）；容忍度默认 3%（`--bt` 可调）。
  - 形态强度分 pattern_score（52~80）；⚠️ 与评分卡置信度不同量纲，不可互换
  - 扫描口径：默认 `--all`（全市场，替代旧「仅上涨股」）；`--risers` 退回旧快模式（仅涨幅 ≥0.5%）。
- 输出：
  - 控制台打印分级候选列表与统计。
  - 同时写 CSV：`./破底翻候选_YYYYMMDD.csv`（utf-8-sig）。
- 依赖：`requests`、`pandas`、`numpy`。建议用 managed Python 的 venv：
  `./venv/bin/python（首次：python3 -m venv venv && venv/bin/pip install -r requirements.txt；WorkBuddy 托管 Python 的 venv 路径随机器/用户名变化，请以自建 venv 为准）`
- 耗时：受网络与线程（8 线程）影响，通常几分钟；失败时脚本会跳过并计入失败数。

**注意**：运行后向用户展示结果（候选清单 / CSV 路径）。CSV 默认落在仓库根目录。

## 约定 1.1：破底翻筛选 · 连接器 fallback（沙箱直连被掐时）

**触发条件**：在 WorkBuddy 沙箱运行 `蔡森破底翻量化筛选器.py` 时，东方财富/新浪/腾讯的直连 HTTP 被网络层掐断（实测 K线取数成功率仅约 8%，281/306 失败），只能得到**有偏抽样、非全市场**结果。

**执行动作（fallback 改用已连接的腾讯自选股连接器 westock-mcp）**：
> 前置：westock-mcp 连接器须已连接（左侧连接器状态「腾讯自选股：connected」）。未连接时此路不可行，直接提示用户在本机跑脚本拿全量。

1. **取上涨股 universe（连接器）**：调 `tool_filter`
   `expression=ChangePCT > 0.5`、`universe=hs`、`market=hs`、`limit=400+`，
   拿到全市场股票代码清单（宽口径，不限于上涨股；替代脚本里被掐的新浪列表 API）。
2. **批量取 K 线（连接器）**：对 universe 代码按每批 ~20–25 只拆分，分批调 `data_kline`
   `codes=...`（逗号分隔）、`period=day`、`fq=qfq`、`limit=250`，拿前复权日 K。
3. **跑破底翻 + 低位检测**：把连接器返回的 K 线喂给脚本的 `detect_podifan()` 逻辑
   （**低位股过滤**：历史分位<30% 且 距年高跌>40% + 破底翻检测），复用同一套评分。
4. **归档**：结果 CSV 仍落仓库根目录 `破底翻候选_YYYYMMDD.csv`（utf-8-sig），并 present_files 展示。

**为什么用连接器**：走独立通道，不受沙箱直连东方财富/新浪/腾讯的代理限流，可取全市场可靠数据，避免有偏抽样。
**注意**：`tool_filter` 只能做财务/价量条件筛选、做不了破底翻形态识别，故 universe 用连接器取、形态识别仍用脚本 `detect_podifan` 在 K 线数据上跑。破底翻必须挑**低位股**（约定1 的长期低位过滤不可省略）。

## 约定 2：M头/双顶「跌幅满足」计算（两头顶中点法）

> **已归位到方法论本体**：完整的「两头顶中点法」规则（MID=(头1+头2)/2、H=MID−颈线、两波等距满足、配色、实战案例、易错点）已写入 `caisen-technical-analysis/SKILL.md` 的「M头/双顶 跌幅满足修正」小节。本约定仅作触发索引，不再重复维护规则细节，避免两处不同步。

**触发条件**：画 M头/双顶（double-top）形态，且需要计算/绘制「跌幅满足」（测幅满足、等幅下跌目标）时，按 `caisen-technical-analysis` 的上述小节执行。

**交付格式**：只用 matplotlib 渲染**静态 PNG**，配合肉眼看图判读形态；不要用 ECharts/Chart.js 等交互缩放组件。

## 约定 3：资本三流每日监控

**触发条件**：用户提到「**看看三流**」「三流」「跑三流」「三流日报」「监控资本流向」「今天钱往哪流」等类似表述（含用户原话「看看三流」）。

**执行动作**：直接运行主副本脚本
`./capital-three-flow/scripts/monitor.py`

**运行方式（运行前须知）**：
- 真实数据（推荐，需 akshare + 用户本机网络）：
  `./venv/bin/python（venv 建法同约定 1） ./capital-three-flow/scripts/monitor.py`
- 离线验证 / 沙箱环境（不联网，用样例数据验证逻辑）：
  上述命令末尾加 `--demo`
- 强制刷新（绕过本地缓存重拉）：加 `--force`
- 起可视化大屏（可选）：加 `--dashboard`（需 `streamlit`）

**脚本要点**：
- 框架：卢麒元「资本三流」+ 费雪方程式 M·V=P·Q，量化监控 流量(规模)/流向(方向·分布)/流速(周转)。
- 三指数：CFCI(流向综合 −100~+100)、CFEI(流转效率)、CRI(走资风险 0~100)；
  共振打分 S = S流量 + S流向 + S流速，S≥2 共振、S≤0 混沌。
- 已工程化补强（贴合用户要求）：
  - **流量「大不大」判定**：M2 绝对量(>300万亿=极大) + 增速基准带(8~11%:扩张/中性/收缩) + 杠杆方向(加/去杠杆)。
  - **流向具体化**：跨境(外储变动→中国↔境外) + A股内部板块多空榜(top5/bottom5)；北向缺失已标注。
  - **微观流速**：宏观 V=GDP/M2 + 杠杆资金日周转(融资买入额/融资余额) + 板块毛周转/集中度HHI。
  - **股票交易参考模块**：把三流翻译为 流动性背景(仓位)/资本流向(多空+板块排名)/微观流速(策略)/综合姿态，输出到报告「四、股票交易参考」段落 + JSON 的 `trading_reference` 字段。纯方法论翻译，非投资建议。
- 数据源：akshare 封装（外管局 M2/外储、央行基础货币、上交所融资融券、同花顺板块资金流 在沙箱可过；
  东方财富系「大盘主力净流入 / 人民币实时汇率」在沙箱被网络层掐断，**需在用户本机跑**才拿得到全量）。
- 依赖 venv：`./venv/bin/python`（venv 建法同约定 1；已装 pandas/numpy/pyyaml/akshare）。
- 产出：主副本 `reports/` 下每日 `capital_three_flow_YYYYMMDD.json` + `.md` 日报。

**注意**：运行后向用户展示结果（日报 md / JSON 路径）。完整说明见主副本 `SKILL.md` 与 `references/`。

## 约定 4：研判评分卡（股票/期货分析强制输出）

**触发条件**：任何「股票 / 期货」类分析——十专家整合、破底翻、蔡森画线、妙想个股、资本三流交易参考等，**凡给出方向性结论**（涨/跌/震荡）的报告或回复。

**执行动作**：结论处必须输出一张「研判评分卡」，凝练不啰嗦，**字段缺一不可且必须可证伪**（"震荡偏多"不带期限是永远不会错的话，禁止）。

> 📐 **完整规格已抽出**：评分卡的 13 项详细规则（基准率 / 量纲校验 / 更新触发器 / 离散三情景 / 可预测性 / 共识矩阵 / 红队 等）统一维护在 **`references/scorecard-spec.md`**（仓库根 `references/`）。下文为一页速查表；**改动评分卡规则一律改该文件**，本约定只保留指针与速查表。

**一页速查表（字段缺一不可 · 必须可证伪）**

| # | 要素 | 要点 |
|---|---|---|
| 1 | 方向判断 | 看多📈 / 看空📉 / 中性震荡➡️ |
| 1.5 | **基准率**（必填，先于置信度） | 无条件 + 条件基准率（outside view）；置信度须从基准率出发调整 |
| 2 | 置信度 | = 三情景主情景概率；展示校准后值；硬上限 = min(数据质量上限, 可预测性硬上限, 无锚定60) |
| 3 | 期限 | 5日 / 20日 / 60日 / 具体季度 |
| 4 | 幅度区间 | 须过 `tools/sanity_check_card.py` 量纲校验 |
| 5 | 证伪位 | 须过 `tools/sanity_check_card.py` 量纲校验 |
| 5.5 | 更新触发器 | 预先承诺的更新规则（互斥穷尽三分支） |
| 6 | 三情景概率 | 离散分布（每情景带收益区间）；由情景树末端路径聚合得出 |
| 7 | 建议仓位 | 由校准后置信度映射 |
| 8 | 凝练逻辑 | 各 1–3 条核心驱动 + 一句话总结 |
| 9 | 已定价检查 | 共识（已在价格里） vs 与共识的差异（variant，值得下注的部分） |
| 10 | 市场先验 | 宏观/事件类必填 Polymarket 赔率；个股类不适用 |
| 11 | 可预测性评级 | 高/中/低/接近随机 → 置信度硬上限 |
| 12 | 共识矩阵定位 | 共识×定价 四象限落格；左上角仓位强制「不动」 |
| 13 | 红队结论 | 最强反证 / 最脆弱环节 / 置信度调整 |

**放置位置**：
- 报告类（HTML/Markdown）：放在核心结论之后，做成显眼「🎯 综合研判评分卡」（参考赛轮轮胎 601058 报告样式）。
- 纯文字回复：在结尾给出同样的九要素，不画图也要写。
- **每条评分卡产出后必须 `python3 predictions-ledger/append.py --json '{...}'` 追加进预测台账**（字段对齐约定4 全部要素（1–13））；每周用 `predictions-ledger/score.py` 复盘判对错、算超额命中率/校准/Brier/CRPS/三色独立性。append 时必须同时传 `--evidence-ref`（证据卡快照路径）与 `--data-quality`（D7 数据质量上限分）；两者缺失的记录在 `score.py` 复盘中会被归入「未记录」行，无法归因。
- **【2026-08-25 加固·闭环红线】append 时必须固化 `entry_price`**（显式 `--entry-price` 或 `tools/save_kline.py` 写入 `market-data-cache/<symbol>.csv` 后由 append 自动读取）；无快照 → append 机器拒绝写入（防复盘时 K线拉不到导致基准 `可用=0根`、超额命中率永远空白）。同时机器强制：① `predictability` 缺失/非法 → 拒绝写入；② `confidence` 超过可预测性硬上限（高85/中75/低65/接近随机55）→ 自动压顶并警告；③ `direction` 限五值枚举（多/空/中性偏多/中性偏空/中性震荡），历史自由文本由 `tools/migrate_ledger.py` 归一。结算优先用 `score.py mark <id> --exit-price <价>`（entry_price 自动算 actual_return）。

**统一规范（已同步）**：
- 用户级长期记忆 `~/.workbuddy/MEMORY.md`「分析输出规范 · 研判评分卡」段。
- 十专家 skill：`caisen-10-experts-analyst/SKILL.md` 第6步「报告结构」1.5 项 + 第6.5步自检清单。
- 跨所有 skill 通用，不限于十专家。

**示例（赛轮轮胎 601058）**：
> 方向：📈 震荡偏多 ｜ 置信度：62/100
> 看多：①关税套利产能放量(柬/印尼/墨) ②提价顺价毛利回升(Q1 +2.13pct) ③估值低位 PE 8–9x
> 看空：①人民币升值汇兑损失(>1亿) ②股东+37%散户化 ③美国关税突变
> 总结：橡胶涨非正相关；赛轮能否涨看产能兑现+顺价，非橡胶价。

> **⚠️ 时间窗口多样性要求**
> 不得让台账里的预测全是 60D。每 10 条预测中至少要有 3 条是 5D 或 20D 短窗口，
> 否则反馈周期长达两个月，校准信号来得太慢，体系无法在合理时间内自我修正。
> 短窗口预测可以用低仓位甚至纸面预测（position 填「不动·仅记录」），
> 目的是拿校准数据，不是拿收益。
>
> **⚠️ 置信度分布约束**
> 每 10 条预测中，至少 2 条 confidence <55 或 >75。
> 若连续 10 条全落在 55-75，说明置信度已退化为中间值锚定，必须在复盘中专项检讨。
>
> **🔒 以上两条已于 2026-08-25 升级为代码硬校验**：`append.py` 在写盘前检查
> 「最近 10 条(含本条) 是否全 60D / 全落 55-75」，违反则**拒绝写入**，逼出短窗口或
> 出界预测来恢复多样性。`--force` 可放行但打 `discipline_violation` 标记，
> `score.py` 第十三节专项统计（见约定13）。

## 约定 5：分析产出「记到」蔡森 skill 目录（2026-07-14 用户要求）

**适用范围**：所有蔡森方法论相关的分析产出——十专家整合、破底翻、蔡森画线、资本三流、妙想个股等。

**执行动作**：
- 📌 `$CAISEN_ROOT` = `/Users/weihaoli/Desktop/蔡森 skill`（**桌面主副本，绝对路径**）；脚本内可用同名环境变量 `CAISEN_ROOT` 覆盖。⚠️ **2026-09-12 校正**：原表述「本 agent.md 所在目录 / 未设置时取脚本所在目录」在**镜像环境**下会解析到 `~/.workbuddy/skills/caisen-10-experts-analyst/`（那里缺 `tools/`、`market-data-cache/`、`analysis-report-template/`，且 `predictions-ledger/` 是一份**内容已漂移的陈旧副本**）——故统一改为绝对路径。**唯一权威副本原则**：脚本、模板、预测台账、知识库一律以桌面主副本为准；镜像内同名目录**不作为数据源、不写入**。
- 工具/模板一律用 `$CAISEN_ROOT/` 根目录内的脚本与 `analysis-report-template/`。
- **HTML 报告存到蔡森 skill 根的 `output/`**（即 `$CAISEN_ROOT/output/{主题}-analysis-{日期}.html`）——这是用户指定的"记到"位置。
- **研判评分卡/预测一律 `append.py` 进 `predictions-ledger/ledger.jsonl`**（同根，约定4已写明）。
- 不要在 WorkBuddy 临时 `output/` 仅留存一份；**必须以蔡森 skill 目录为权威归档副本**。WorkBuddy 副本可保留作预览，但落盘归档归位到蔡森 skill。

**注意**：WorkBuddy 会话里的 deliverable 仍用 present_files 展示，但落盘归档地址固定为蔡森 skill 目录。

## 约定 6：分析方法论偏好 · 因果链优先 + 事件发散推演（2026-07-21 用户要求）

**触发条件**：任何「股票 / 期货 / 宏观 / 商品」类市场分析（十专家整合、破底翻、资本三流、妙想个股等，凡做方向性研判的分析），且在约定4评分卡之外，用户要求**重视因果传导、对可能性先做发散再推导**。

**核心要求（默认分析风格，无需每次确认）**：
1. **因果链优先**：任何结论都要落到 "X→Y→Z" 的传导机制，不拍脑袋、不只给方向。区分四层——「已确认事实 / 历史验证 / 当前差异 / 对盘面的含义」。
2. **事件发散（情景树）**：先列关键不确定变量（天气、政策、资金、供需拐点…），对每个变量发散 乐观/基准/悲观 多情景，搭成情景树，再逐分支推导。
3. **逐分支因果推导**：每个发散情景都推到「最终对价格/资产的含义」，并给**可证伪触发条件/证伪位**（如"站回8300则判断失效"）。
4. **证据分级标注**：因果链节点标证据强度（confirmed / historical / hypothesis / unverified）；未激活机制标"验证中"，不冒充已发生。

**交付体现**：报告/回复中至少出现
- 一张**因果链图或文字链**（机制传导，分层标注证据强度）；
- 一张**情景发散树**（关键变量→多情景→各分支因果→价格含义+证伪位）。
商品期货（如天气→供需链：厄尔尼诺→南疆高温→坐果→减产→期价升水）尤其适用本约定。

**与现有 skill 关系**：叠加在 `empirical-causal-analysis`（因果识别/反事实）、`serenity-method`（供应链瓶颈 OSINT）、`caisen-10-experts-analyst` 十专家框架之上，作为默认风格而非替代。

**统一规范（已同步）**：用户级长期记忆 `~/.workbuddy/MEMORY.md`「分析方法论偏好 · 因果链优先 + 事件发散推演」段。

## 约定 7：桌面主副本原则（2026-07-21 用户硬性规定）

**原则：桌面 `蔡森 skill/` 目录永远是最新、权威的主副本（master）；`~/.workbuddy/skills/` 仅为镜像（mirror，供 Skill 工具注册调用）。两处必须保持同步，桌面优先。**

**执行动作**：
- 任何对 `mi-analysis`、`caisen-10-experts-analyst`、`caisen-technical-analysis`、`capital-three-flow` 等技能的逻辑修改，**先在桌面主副本改，再镜像同步到 `~/.workbuddy/skills/`**，不得只改镜像让桌面落后。（旧 `caisen-9-experts-analyst` 已归档至 `_archive/`，不再维护。）
- 新建技能同样：桌面建主副本 → 复制一份到 `~/.workbuddy/skills/` 注册。
- 若发现镜像与桌面不一致，**以桌面为准**回写镜像。
- **⚠️ 副本唯一化**：同一份知识库/规则文件在仓库内只允许存在**一个权威副本**（如底层逻辑知识库唯一权威副本为根目录 `base-logic/`）。禁止在技能目录内再复制一份「内嵌副本」；历史重复副本一律 `git mv` 进 `_archive/` 留档，引用统一改指权威副本。跨技能共用的文件（如 `source-tiers.md`）指定唯一维护处，其余位置只写引用不落拷贝。

## 约定 8：Mi姐「趋势·筹码·情绪」交易战术框架（第十专家）

**来源**：GitHub `yanboim/Mistery` 的 Mi姐股市文集，已 operationalize 为可运行框架（`mi-analysis` 技能）。

**触发条件**：用户提到「**跑Mi姐**」「**Mi姐分析**」「**回测Mi姐口诀**」「趋势筹码情绪框架」「Mi姐买卖点」等，或需要在杨/卢/蔡/笨鸟/白毛定性结论之后落地**可执行买卖点+仓位+情绪纪律**时。

**两个技能及其分工**：
1. **`mi-analysis`（方法论本体，可直接跑）**：
   - 根模型 = 三周期共振（大方向·中结构·小时机）× 三类资金博弈 × 交易三要素（情绪/筹码/时间）。
   - 模块：A 趋势(价>250日线+均线多头+周线>60周) / B 结构(缩量回踩MA20=健康回调,破MA20×0.95放量=破位) / C 时机(缩量回踩MA5买,破20日线/浮亏6%/持仓>6日不涨=卖) / D 资金博弈 / E 情绪六阶段 / F 仓位纪律(牛20%·熊5%·震荡10%单标)。
   - 运行：
     - `framework.py` 单股跑信号；`backtest.py` 回测（沙箱用新浪 getKLineData；全市场需本机跑）。
     - `scan.py --codes sh600519,sz300750` 每日扫描；带 7 天本地缓存。
   - ⚠️ 回测铁律：纯均线回踩(基线ABC)跑输基准；真正贡献 alpha 的是 **D/E 资金情绪门控** + **避冰点高潮**。框架禁止退化为纯均线买卖。
2. **`caisen-10-experts-analyst`（十位圆桌）**：Mi姐 为**第一梯队核心定性第六位**（与杨/卢/蔡/笨鸟/白毛并发调度），角色「扣扳机+压注」；妙想→第七、实证→第八、风控→第九、腾讯圆桌→第十。
   - **触发市场类分析时调用 `caisen-10-experts-analyst`，不要再调用旧的 `caisen-9-experts-analyst`**（旧版已归档至 `_archive/caisen-9-experts-analyst/`，仅备查，不再主动使用）。

**与既有体系互补**（详见 `Mi姐框架_交叉验证.md`）：资本三流定风向 → 蔡森定靶心(形态满足位) → Mi姐定扣扳机+压注。三者不重叠、天然互补。

**统一规范（已同步）**：用户级长期记忆 `~/.workbuddy/MEMORY.md`「Mi姐交易战术框架」段。

## 约定 9：向心坍缩宏观剧本 · 分析背景资料（2026-07-24 用户录入）

> 以下为「向心坍缩（capital flowing inward / 资本向核心收缩）」剧本的宏观背景素材，录自用户 2026-07-24 整理的地缘+汇率+资产+政策+债务+流动性全谱快照。
> **用途**：后续任何宏观/大类资产/十专家整合/资本三流研判，应以此作为"已确认事实基底"与"阶段进度基准"，不必每次重新检索；新数据与之比对、更新进度即可。
> **属性**：这是用户的剧本框架（卢麒元体系衍生），属"背景资料"，非 agent 自创观点；引用时标"用户向心坍缩剧本"。

### 9.1 七大维度事实快照（截至 2026-07-24）

**① 能源与地缘：霍尔木兹再封锁，油价"二次拉升"**
- 2026年7月中东升级：伊朗宣布关闭霍尔木兹海峡，美军随即军事打击。
- 7月12日伊朗实施关闭海峡极端举措；至7月10日海峡流量已下滑至6月18日签谅解备忘录前的水平。
- 美伊互相导弹袭击，胡塞武装威胁攻击红海船只。
- 油价：WTI 7/13–17当周 **+15.52% 至 82.49 美元/桶**；布伦特曾于4月底升至约 **126 美元**。
- 分析师预警：冲突扩大则油价或再破 **100 美元/桶**。
- 定位：第一阶段关键信号——油价从"磨底"转向"启动"，尚未进入"暴力拉升"。美国战略石油储备未耗尽（"百日窗口"逼近）。

**② 汇率与资本流向：日元崩盘，新兴市场"失血"**
- 日元：7月21日 美元兑日元一度跌破 **163**，创1986年12月以来近40年新低；过去一月自160走弱至163区。
- 驱动：美日10年期国债利差远超历史均值，套息交易压低日元。日财务省4–5月动用创纪录 **11.73万亿日元（约730亿美元）** 干预，效果仅维持数周。
- 新兴市场：美元指数升至101.18附近（选举日以来+2.1%，近一年最高）；韩元兑美元刷新历史最低，印度卢比承压。"美元债务死亡循环"。
- 人民币：报 **6.7773**，相对稳定。中国股票基金7/9当周净流入 **165.4亿美元**（2025年以来97.5%分位），但增量几乎全由国内资金+被动工具贡献，海外长线仍连续净流出。
- 定位：第二阶段前兆——非美货币系统性贬值，资本撤离外围。日元已倒下，新兴市场承压，人民币是唯一例外。

**③ 资产价格：避险齐涨，风险承压**
- 黄金：7月21日突破 **4,080 美元/盎司**；与美元、美债收益率同步上涨（反常，避险需求压倒利率因素）。
- 美股（7/13–17）：道指 **−0.65% 至 52146.42**；标普500 **−1.14% 至 7457.69**；纳指 **−2.62% 至 25520.24**。AI板块"吸血"接近尾声，轮动发生。
- A股（同期）：上证 **−6.75% 至 3764.15**；深成指 **−10.99% 至 13706.88**——跌幅远大于美股（承受外围压力）。
- 美债：10年期逼近 **4.6%**，未破6%红线，趋势向上。
- 定位：黄金与美元同步涨（反常）；美股A股同步跌（A股更狠）；"金转油"尚未完全启动，但"短股长金"已发生。

**④ 美联储政策：沃什"明缩暗扩"**
- 美联储资产负债表自2007年<9000亿美元膨胀至约 **6.7万亿美元**。
- 沃什希望缩表，但称"18周远远不够"；缩表计划预计年底前推，意见征询至2027Q2，实际缩表明年Q4开始。
- 加息预期：104位经济学家一致预计7月维持 **3.50%–3.75%**；78位预计年底前不变；44位认为再加息风险较高，鹰派重燃。
- 定位：第二阶段启动必要条件——高利率维持足够久，完成"资本速冻"。

**⑤ 债务与违约：美国消费端"爆雷"**
- 信用卡债务创纪录 **1.25万亿美元**；90天以上逾期率 **13.12%**（15年新高）。
- 汽车次贷60天以上逾期率 **6.8%**（32年最高）。
- 学生贷款违约人数约 **950万**，违约债务 **2333亿美元**，其中57%同时拖欠信用卡。
- 商业地产整体逾期率 **4.02%**，CMBS逾期率 **7.55%**。
- 高收益债违约率 **1.75%**，杠杆贷款 **3.0%**（预计2027年升至4.5%）。
- 定位：底层债务断裂，尚未传导至系统重要性金融机构；从"外围"向"核心"蔓延前奏。

**⑥ 流动性：全球货基"大撤退"**
- 7/9–15当周全球货币市场基金单周净流出 **984.6亿美元**（分位数骤降至1.2%极低）。
- Michael Howell：追踪全球流动性动能核心指标已于2025Q4触顶，此后持续减速。
- 定位：流动性从"现金工具"向"风险资产"切换（非健康信号，是资本找最后"高收益容器"的挣扎）；动能触顶后下一步即"速冻"。

### 9.2 向心坍缩进程综合评估（阶段进度）

| 阶段 | 核心特征 | 当前状态 | 进度 |
|---|---|---|---|
| 第一阶段：定向导流 | AI泡沫蓄水、大宗磨底 | AI显疲态，油价启动 | ≈70% |
| 第二阶段：资本速冻 | 油价暴涨、流动性枯竭、非美踩踏 | 油价二次启动、日元崩盘、新兴市场承压 | ≈30% |
| 第三阶段：向内收缩 | 资本回流美国、实物资产重估 | 黄金破4080，但未见"最后一跌" | ≈10% |

**关键观察指标（按优先级）**：
1. 油价能否破 **100美元**（当前82，空间+22%；霍尔木兹持续封锁则大概率）。
2. 10年期美债收益率能否破 **5%**（当前4.6，空间+40bp；破5%触发更广重定价）。
3. 日元是否跌破 **165**（当前163，高盛已上调预期至165；破165引发更大套息平仓）。
4. 黄金是否现"最后一跌"（当前4080；理论：期货黄金短期被不计代价抛售，随后实物止跌；尚未见信号）。
5. 美联储是否转向加息（当前定价7月不加息，但44%经济学家认为加息风险较高；通胀超预期则升温）。

**整体判断**：向心坍缩按剧本推进，尚未进入最剧烈阶段。油价二次启动刚开始，日元已倒下，新兴市场承压。决定"速冻"能否完成的关键变量＝油价破100 + 美债破5% + 联储被迫重启加息；任一触发都可能加速进程。

> 关联约定：本背景资料与约定3（资本三流监控）、约定6（因果链+发散推演）、约定8（Mi姐框架）联动使用；宏观研判须套用约定4评分卡，并按约定5归档至蔡森 skill `output/`。

## 约定 10：数据采集规范 v2（数据层默认流程）

所有市场分析的第0步一律走 `caisen-10-experts-analyst/references/00-data-collection-v2.md` 的 D0–D9：D0 数据日历前置、D1 T0 实物层、D1.5 商品期货期限结构三件套（月差/基差/注册仓单+前20席位持仓，商品期货第0步必采硬数据，缺任一项置信度封顶65）、D2 五面镜子（钱/货/人/单/嘴）、D3 五连想+定向取证（即第0.8步）、D4 反向取证、D5 代理变量有效性检验、D6 取数失败率入账、D7 数据质量分→置信度上限封顶、D8 证据快照绑定台账、D9 一手源扩容。第0.8步五连想为 L2 及以上必做；最终置信度受 D7 数表封顶，评分卡 `append` 时带 `evidence_ref` 和 `data_quality`（见约定4）。

## 约定 11：markdown 断链检查（2026-07-29 建立）

- 全仓 markdown 链接/反引号路径的静态断链检查器：`tools/check_links.py`（stdlib-only，退出码 0=通过 / 1=断链）。
- 已知缺失引用基线：`tools/check_links.allowlist`（按「相对路径:行号」精确跳过，行号漂移会重新触发检查）。分类：上游第三方 skill 引擎/文档未随本仓库分发、运行时生成的数据文件、早期已废弃的桩文件路径。
- 跨技能引用优先指向兄弟技能的**权威副本**（如 `$CAISEN_ROOT/lukou-daye/references/...`、`$CAISEN_ROOT/benniao-analysis/references/...`、`$CAISEN_ROOT/serenity/.../references/...`），遵循约定7「副本唯一化」，不在各自 SKILL.md 内重复拷贝参考文档。
- 提交前钩子已安装（`.git/hooks/pre-commit` → `tools/pre-commit-check-links.sh`）：存在非 allowlist 断链则阻止提交，禁止引入新断链。新克隆需重装钩子：`ln -s ../../tools/pre-commit-check-links.sh .git/hooks/pre-commit`。

**⚠️ 残留检查纪律（2026-09-12 补充 · 已两次踩坑）**：
1. **编辑被 allowlist 覆盖的文件后，必须重跑 `tools/check_links.py` 并同步行号**。allowlist 按「相对路径:行号」精确匹配，被覆盖文件的行号一旦漂移，豁免立即失效 → 钩子会误报断链阻止提交。做法：按 allowlist 注释里记录的漂移轨迹同步行号，并在注释中留痕。**跑检查一律从仓库根目录执行**（`cd "/Users/weihaoli/Desktop/蔡森 skill" && python3 tools/check_links.py`）；在子技能目录下跑会 `No such file or directory`。
2. **macOS 自带 BSD grep 的 `\|` 交替语法是静默假阴性**（`grep "a\|b"` 会漏匹配而非报错）。做「关键词/旧表述是否清干净」的残留检查时，**禁用 BSD 的 `\|`**：改用 `grep -E "a|b"`，或用 Python 子串校验（`python3 -c "..."`）。历史教训：一次「无残留」的结论靠 BSD grep 得出，改用 Python 复核后发现 4 处真残留（行 469/514/657/745）。
3. 破坏性清理一律先**备份到 `_archive/`**（`cp -R`）再 `/usr/bin/trash` 非破坏删除；`_archive/` 已被 `.gitignore` 排除，不入版本控制。删除后做一次**镜像同步 + md5 校验**（约定7）。

## 约定 12：结论输出四段式——理论→数据→前提→结论（2026-08-25 用户要求）

**触发条件**：所有给出方向性/宏观判断的分析回复（十专家整合、路口大爷、资本三流、宏观/商品/股票研判等），在约定4评分卡与约定6因果链之外，**结论部分必须按四段式结构呈现**。

**执行动作（默认格式，无需每次确认）**：
1. **用到的理论**：先列本次判断依据的基准理论/规律（如库存-价格反向规律、TGA 账户理论、政治经济周期理论等），说明"正常市场应该怎样"；
2. **用到的数据**：列出支撑判断的核心数据，分类呈现（财政类/商品类/航运类/政治地缘类等），标注数据时点；
3. **前提（推导链条）**：大前提 = 理论规律；小前提 = 数据事实；重点标出**数据与规律的背离**（背离即反常信号）；
4. **结论**：明确判断 + 后续可追踪的验证指标。

**格式范本**：`lukou-daye/examples-0825.md`（2026-08-25 选举周期×市场失真×美元战略全套推导，用户录入）。
**与路口大爷的关系**：该格式与路口大爷"三层四维"推导天然契合，已作为整体方法论启示写入 examples-0825.md 末节；今后路口大爷类分析输出一律按此结构。

**统一规范（已同步）**：用户级长期记忆 `~/.workbuddy/MEMORY.md`「分析输出规范 · 结论四段式」段。

## 约定 13：组合层风控 + 左侧加仓数学 + 错因分类（2026-08-25 方法论评审落地）

> 经济学大师评审结论：分析层已达个人研究员天花板，真正的短板在**组合层（相关性）、资金层（加仓数学）、执行层（纪律进代码）**。本约定把三大短板固化成可执行的规格与脚本，补全体系。

### 13.1 组合层风控（多品种共享因子 → 同穿风险）

**核心逻辑**：研究品种（锰硅/苹果/工业硅/PVC/氧化铝/红枣/尿素/白糖等）全部共享中国内需/地产链/政策预期等宏观因子。每个品种的证伪位独立设置，但**一个宏观事件会同时击穿所有相关证伪位**（LTCM 死法：单笔都有理，叠加成灾难）。评分卡只管"这笔对不对"，没人管"这一篮子长什么样"。

**执行动作**：
- 每条预测 `append.py` 时**必须**填 `factor_exposure`（该预测暴露的共享宏观因子标签，如 `china_demand` / `property_chain` / `policy_expectation` / `usd_credit` 等；多标签用逗号分隔）。缺字段 → 机器拒绝写入（与 entry_price 同等级红线）。
- 开仓前跑 **`predictions-ledger/portfolio.py`** 看组合总账：按 `factor_exposure` 归类 open 预测，输出净方向、因子集中度。
- **集中度硬规则**：同一因子标签下 open 预测 ≥ 5 条 → 新开同因子仓强制**降一档**（置信度压 5 点 / 仓位减半）；≥ 8 条 → 直接禁止新开，先平旧仓。
- 完整规则见 **`references/portfolio-risk-spec.md`**（因子归类表、降档规则、LTCM 式同穿案例）。

### 13.2 左侧加仓数学（"越跌越买"必须形式化）

**核心逻辑**：左侧加仓=价格越跌仓位越重、浮亏加速放大。入场分析（蔡森定靶心+Mi姐扣扳机）极强，但加仓路径无任何 skill 管。必须把"便宜不是买入理由，原逻辑仍成立才是"写进数学。

**执行动作**（开抄底仓前必读 **`references/left-side-averaging-spec.md`**，含完整算例）：
- ① **预定格数**（默认 3 格），把"从入场到证伪位"的距离等分；
- ② **每格资金递减**（1/2、1/3、1/6 或凯利派生，**绝不等额**）；
- ③ **总亏损预算**（这单抄底最多亏总资金 X%，到线认错，不止损等于没策略）；
- ④ **格间证伪**：跌破第 2 格 = "原逻辑错了"，不是"更便宜了"，停止加仓转认错；
- ⑤ **置信度→格数/仓位映射表**（高置信 3 格满仓 / 中置信 2 格半仓 / 低置信 1 格轻仓），避免情绪加仓。

### 13.3 错因分类（miss 不是"错了"，是"哪种错"）

**核心逻辑**：已判定样本里 4 miss + 0 hit，但黄金实际 +15.7%、S&P +3.1% 仍是 miss——这大概率是两种完全不同的错，药完全不同：
- `direction_wrong` 方向错（换方法）／`timing_wrong` 时序错（方向对但太早，改节奏不改方向）／`magnitude_wrong` 幅度错（改目标区间）。

**执行动作**：
- 复盘 `score.py mark <id> --status miss --miss-type direction_wrong|timing_wrong|magnitude_wrong` 必须带 `--miss-type`；
- `schema.json` 已加 `miss_type` / `factor_exposure` / `discipline_violation` 三字段；
- `score.py` 第十三节专项统计错因分布 + 纪律违规（连续全 60D / 全 55-75），给出"换方法/改节奏/改区间"的对应建议。

### 13.4 组合总账脚本

**运行**：`python3 predictions-ledger/portfolio.py`（默认读 `ledger.jsonl`；`--threshold 5` 调集中度阈值；`--factor china_demand` 只看单一因子）。
输出：各因子 open 条数、净方向（多/空敞口）、集中度告警；供开仓前一眼看穿"篮子长相"。

**与约定4关系**：约定4管单笔评分卡，本约定管"单笔加起来"的组合与资金纪律——两者互补，不重叠。

**统一规范（已同步）**：用户级长期记忆 `~/.workbuddy/MEMORY.md`「分析方法论 · 组合层/加仓数学/错因分类」段。

## 约定 14：提交与推送纪律（2026-09-12 建立）

**背景**：本仓库位于 macOS `~/Desktop`（受 TCC 保护），`.git/*.lock` 偶发无法 unlink；且分析会话常有并发进程写台账。两者都会让 git 的输出产生误导。

**执行动作**：
1. **推送结果一律用 `git ls-remote origin main` 对照 `git rev-parse HEAD` 验证，不要信 `git push` 的输出。** 已知假象：push 可能报 `update_ref failed for ref 'refs/remotes/origin/main'` 或 `Everything up-to-date`，但**远端其实已收到对象并更新**——失败的只是本地 remote-tracking ref（被残留 `origin/main.lock` 挡住）。只看 push 输出会误判成"没推上去"而重复操作。
2. **写 `.git/index.lock` / `refs/remotes/origin/*.lock` 报 `Operation not permitted` 属已知噪音**：不阻塞 add/commit/push。必要时用 `dangerouslyDisableSandbox` 的 `rm` 清一次；**即便清不掉也不要反复重试**，继续操作即可。
3. **提交 `predictions-ledger/ledger.jsonl` 前，先隔几秒 `wc -l` 对两次**，确认无并发会话正在写入，避免提交到半行 JSON。本仓库存在多会话/自动化并发追加台账的情况。
4. 提交前固定三件事：`rm -f .git/index.lock`（清残留）→ 跑 `tools/check_links.py`（或直接依赖 pre-commit 钩子）→ 提交后再跑一次镜像 `md5` 校验（约定7）。

## 约定 15：ocmacro 定向取数（分析框架强制数据源 · 2026-09-12 建立）

**触发条件**：`caisen-10-experts-analyst` 框架的**所有 L2 及以上分析**（即除纯事实查询外的任何「分析/研判/方向/标的」类问题）——**无条件必跑**，不限于美国政策或地产主题。

**为什么单列一条约定**：这是目前**唯一**能提供「政策回撤基准率」与「地产历史周期分位」的数据源，市场上无免费替代品。它同时喂到路口大爷（政策事件概率面）、笨鸟（M35 泡沫定位 + M47 利率归因）、卢麒元（化债→地产链月度与高频证据）、蔡森/Mi姐（地产链价量确认）、实证因果（事件研究样本框）、红队（外部基准率），是本框架的**广谱基础设施**，故不设为条件触发。

**执行动作**：

```bash
cd "$CAISEN_ROOT/ocmacro-feed"          # $CAISEN_ROOT=/Users/weihaoli/Desktop/蔡森 skill
python3 ocmacro_feed.py digest          # 人读证据卡（默认）
python3 ocmacro_feed.py digest --json   # 机器可读，落盘 data/digest-<日期>.json
python3 ocmacro_feed.py snapshot        # 全量抓取（约 6MB，含 BIS 950KB / 30城 700KB）
```

**必取四类事实**（缺任一项须在报告写明「该项未取到」）：
① TACO 当前值/分位/20日变化/状态　② 条件概率表（5/10/20日 × 四档）**+ 无条件基准率**
③ 主导因子（`dominantDriver` + `dominantShare`）　④ 地产四件套（70城同比 + 能级环比连正月数 + BIS 中国回撤与同类周期中位 + 30城高频52周对齐同比 + 同比上涨城市数）

**七条铁律（违反即视为分析不合格）**：

| # | 铁律 | 反例（真实踩过） |
|---|---|---|
| 1 | **TACO 是压力存量，不是领先指标**；回撤落地后存量被消耗，指数见顶回落 ≠ 风险解除 | 用「指数还没涨起来」推断「暂时不会有事」 |
| 2 | **条件概率必须用「回撤日」口径**（事件 `period` 内**最后一个**日期），不得用威胁日 | 用威胁日使 ≥+8 档 20 日概率从 100.0% 退化成 97.0%、+5~+8 档从 81.2% 退化成 62.5%；另若不把回撤日映射到交易日（周末事件），事件会静默丢失、20 日基准从 62.7% 掉到 58.7% |
| 3 | **报概率必须同时给基准率**；≤+5 两档与基准无显著差异，不得当安全信号 | 只报「100%」而隐去 62.7% 的基准率，严重高估信息量 |
| 4 | **分项贡献拆两列**：`contributions` 判水平、`changeContributions` 判这轮驱动；「什么驱动的」看**变化列**最大值 | 拿水平列最大值当主导因子（实锤：两者都指向 10Y，但真正的信息是变化列 75.2% 的占比，不拆看不出来） |
| 5 | **样本量警告随行**，一律写「参照，不作铁律」 | 33 个 ≥+8 样本日高度聚集在 2026 年，有效独立样本远小于朴素计数 |
| 6 | **地产是本地快照不是实时**，报告须写明快照日期（令牌型接口 TTL≈2h） | 把快照日期当成最新数据期 |
| 7 | **数据只提供事实层**，方向性结论一律由方法论独立推导；站点作者战绩为自述、未经独立验证 | 由数据卡直接推出交易建议；引用自述战绩当已证实 |

**冲突处理**：ocmacro 只给事实，**方法论优先**。其读数与其他专家结论矛盾时（如「TACO 高位＝必然回撤」vs 期货团「地缘溢价仍高」），必须**显式标注分歧**，不得静默取其一。

**切片纪律**：遵守第2步「输入切片」约束，按专家分片给（路口大爷给政策面、笨鸟给 10Y+BIS、卢麒元给地产链、蔡森/Mi姐给价量、实证因果给事件库），**不得把全量复制给所有子智能体**。

**降级**：抓取失败 → 标注「ocmacro 不可用，政策压力面留空」，**严禁用记忆里的旧读数冒充当前值**；本地有旧快照时注明快照日期。

**权威文件**：`ocmacro-feed/SKILL.md`（解读铁律 + JSON 结构 + 勘误记录）；框架侧落在 `caisen-10-experts-analyst/SKILL.md` 第-0.4步。

**统一规范（已同步）**：
- 框架：`caisen-10-experts-analyst/SKILL.md` 第-0.4步 + 权重体系数据基础设施层 + 第0.8步「去哪查」表 + 笨鸟/卢麒元/路口大爷/红队各章节 + 第4步时效分频 + 第6.5步自检项 + 约束「必须做」。
- 项目长期记忆：`.workbuddy/memory/MEMORY.md`「ocmacro 数据源接入」段。

---

## 约定 16：板块间联动关系 · 状态依赖型虹吸法（新技能 `sector-linkage-analysis` · 2026-09-16 建立）

**触发**：用户问「X 板块起来，Y 板块会怎样」这类**板块传导 / 资金分流 / 跷跷板 / 风格轮动**问题（非单一标的研究）。

**技能位置（桌面主副本优先）**：
- 主副本：`/Users/weihaoli/Desktop/蔡森 skill/sector-linkage-analysis/`（`SKILL.md` + 可运行脚本 `linkage.py`）
- 镜像：`~/.workbuddy/skills/sector-linkage-analysis/`（Skill 注册用）
- ⚠️ 两处需保持同步，桌面优先；md5 校验一致。

**核心方法论（七步）**：
0. **先验证前提**——用户问「X 起来」时，X 当前常常**没起来**。前提不成立 → 结论直接是「条件不成立，不做」。
1. 取 X + Y1 + Y2 + **基准（沪深300，不可省）** 的周线，算 4 周滚动收益。
2. 筛 X 涨幅 ≥ 阈值（2σ 或固定 12%）的事件。
3. **关键分组**：按同期基准是否涨 > 3% 二分 → **增量普涨型**（有增量，水位上抬）vs **存量独立型**（零和博弈）。两组 Y 收益**符号相反** = 状态依赖型虹吸。
4. **反证检验**（必做，不做则分析不成立）：筛 X 跌幅 ≥ 阈值，看 Y 是否同向跌 → 同向 = **双向跷跷板否证，只存在单向虹吸**。
5. **相关结构三件套**：原始相关（含 beta，应全正）vs 剥离 beta 的超额相关（应转负）→ **符号反转 = 因子重叠最硬证据**；Y1−Y2 超额相关显著为正 = **同一因子，须合并仓位**。
6. **波动率定标（口径铁律）**：σ 必须用**单周** σ_1w，再按 √horizon 缩放到持有期 → `σ_H = σ_1w × √持有周数`；目标区间 = 1σ_H；**证伪位最小距离 = 1.5σ_H**，小于此值的研判会被噪声打掉、无效。**错误示范**：拿 W 周滚动 σ_W 再乘 √horizon（σ_W 已含 √W，会重复放大）。
7. 当前状态定位 → **风险回报比 < 1:2 给 0% 仓位**，写明「不是看空，是条件不成立」；同因子板块合计仓位上限，不叠加。

**与既有约定的关系**：本约定产出仍须套用**约定4 评分卡**（不给概率，用胜率 n/N 频次）、**约定12 结论四段式**、**约定13 组合层风控**（同因子合并计算仓位）。

**实战结论存档（2026-09-16，半导体 × 房地产 × 养殖 × 沪深300）**：
- 增量普涨型 n=11：半导体 +18.9% / **房地产 +8.5%（中位 +3.2%，胜率 91%）** / **养殖 +4.0%（中位 +1.7%，胜率 82%）** / 沪深300 +7.2%
- 存量独立型 n=5：半导体 +19.0% / **房地产 -5.5%（胜率 20%）** / **养殖 -6.4%（胜率 0%，0/5）** / 沪深300 -1.0%
- 反证（半导体跌 ≥12%，n=11）：房地产 -2.3%（胜率 27%）、养殖 -1.3%（胜率 36%）→ 同向跌，**跷跷板否证**
- 超额相关：半导体−房地产 **-0.182**、半导体−养殖 **-0.307**、**房地产−养殖 +0.356**；原始相关全正 → **符号反转**
- 长期（2019-06-14→2026-09-16）：半导体 +316.5% / 房地产 -50.5% / 养殖 -43.4% / 沪深300 +22.6%
- 结论：**房地产与养殖是同一因子**（须合并仓位，合计 ≤10%）；两者均 **0% 仓位**（条件不成立，非看空）；二选一选房地产。关键价位：房地产触发 **1.215** / 止损 **1.169**；养殖触发 **0.533**
- 独立复测（`linkage.py` 跑 2021-09-17→2026-09-16 周线，threshold=12）：状态差 房地产 **+12.89pct** / 养殖 **+10.33pct**，超额相关 -0.156/-0.284/+0.356 → **方法与结论双向复现**

## 约定 17：蔡森 K 线形态出图引擎接入（`caisen-kline` · 2026-09-18 建立）

**来源**：GitHub `h2340063/caisen-kline` v1.0.2（MIT · 作者洪老师 · 免费仅供学习交流）。把蔡森「A 招颈线识别 + B 招等幅满足 + K 招失败识别」做成可运行的几何识别流水线，输出 4K 中文标注图 + 结构化文字。

**定位**：**不是第十三位专家，是 🟠 蔡森席位的技术形态出图执行器**——引擎给几何证据，蔡森专家给方向解读；它**不自带方向性判断**。

**技能位置（桌面主副本优先）**：
- 主副本：`/Users/weihaoli/Desktop/蔡森 skill/caisen-kline/`（即 `$CAISEN_ROOT/caisen-kline`）
- 镜像：`~/.workbuddy/skills/caisen-kline/`（Skill 注册用）
- ⚠️ 两处需保持同步，桌面优先；**2026-09-18 全目录 md5 校验一致**。

**接入点（已写入 `caisen-10-experts-analyst/SKILL.md` 五处）**：
1. frontmatter `description` 增加触发说明；
2. 顶部子技能清单增加 `caisen-kline` 条目；
3. 权重体系 🟠 蔡森条目下增加「📉 技术形态出图与几何验证 → caisen-kline」子项；
4. 新增专章「📉 caisen-kline 技术形态出图引擎（蔡森技术面执行器）」——含触发条件表 / 两条调用路径 / 输出物 / 支持范围 / 四条使用纪律 / 合规边界；
5. 「错误处理与降级」增加 caisen-kline 失败三级降级链。

**触发条件（命中任一即调用）**：① 用户给标的代码/名称并要求形态分析（「分析 600460」「颈线在哪」「假突破」「破底翻」「多空转折」）；② L3/L4 报告需蔡森技术面配图；③ 蔡森专家已给形态结论需几何证据复核。**禁止凭空画线或用示意图替代**。

**硬前置**：`tdx-connector`（通达信）已连接 —— 无替代方案，未连接则完全不可用。

**本机调用命令**（已验证跑通，勿另建 venv）：
```bash
VP=/Users/weihaoli/.workbuddy/binaries/python/envs/default/bin/python   # pandas 3.0.3 / matplotlib 3.11.0 / mplfinance 0.12.10b0
cd "$CAISEN_ROOT/caisen-kline"
$VP run_from_tdx.py <tdx结果文件> <代码> "<准确名称>" "$CAISEN_ROOT/output/caisen-kline"
```
- tdx 大结果会自动落盘（超出 token 上限时工具返回文件路径），**直接把该文件喂给引擎即可**，无需手工转 JSON；
- ⚠️ `tdx_kline` 的 `target` 参数已被连接器移除（2026-09-08 实测），带上会报 `additionalProperties` 错；
- 统一输出目录 `$CAISEN_ROOT/output/caisen-kline/`（该目录已在 `.gitignore` 中，图不入库）。

**支持范围（2026-09-18 实机复核）**：✅ A股（沪深创）/ 港股 / 中证指数 / 沪深 ETF（含跨境 QDII）/ 商品期货（上期所·大商所·郑商所）；❌ **硬拒绝、绝不静默出图**：美股 / 外汇 / 中金所 / 上期能源 / 广期所 / 北交所 / 债券（含可转债）。

**四条使用纪律**：① 引擎给几何、专家给解读，**禁止把引擎目标位直接写成操作建议**；② 形态识别错会连锁错（颈线错→H 错→目标错），复杂/长期整理结构须人工复核颈线触点是否同一水平带；③ **S14 未实现**——形态进行中、已达目标①未破位时不会提示「分批止盈/止损移至成本」，实盘须自行补上；④ 无回测、无胜率，**禁止引用为「胜率 XX%」**（与诚实度协议一致）。

**验收记录（2026-09-18 实机）**：
- `selfcheck.py` **5/5 通过**（Python 3.13.12 / 三依赖齐 / 包完整 / CJK 字体探测到 Hiragino Sans GB, STHeiti, PingFang SC, Arial Unicode MS / 离线端到端出图成功）；
- **14 套回归测试 14/14 全绿**（pandas 3.0.3 无 API 兼容问题）；
- **真实数据端到端**：士兰微 600460 日线 250 根（前复权）→ 出图 **4124×3915 px / 1.0MB** + 结构化文字。识别结果：**W 底**，颈线 **38.95**（2026-05-28 高 39.06 / 2026-06-16 高 38.83 双触点），形态底 **24.62**，H=14.32，目标① **53.27** / 目标② **67.59**；**目标① 已于 2026-06-26 兑现（最高 57.02）**，现价 32.77 跌破颈线 15.9% → S13 判定为「**完成后回撤**」（**非**假突破失败），周线/月线均震荡、无大方向背书。

**与既有约定的关系**：本引擎产出仍须套用**约定 4 评分卡**、**约定 12 结论四段式**、**约定 13 组合层风控**；其目标位属几何外推，**不得替代**评分卡中的胜率 n/N 频次。

**口径陷阱（已归档）**：① 512480 ETF 于 2026-07-02 登记 / 07-03 除权按 **1:2 拆分**，`data_kline` 传 `fq:""` 复核无跳空 → 接口 qfq 序列已自动调整，误判会把年线算成「空头」；② 「半导体主力净流入 43 亿」是**全市场行业板块口径**，与 512480 单只 ETF +2.83 亿**不可混用或相加**。

---

## 约定 18：蔡森出图配色规范——四色K线（EMA12/50）+ 量价（2026-09-20 用户定稿）

**触发条件**：**任何**「蔡森画图 / 出 K 线形态图 / caisen-kline 出图」的场合。用户原话：
> 「蔡森每次画图，用这个（Pine v5 四色K线指标），要看量价」

**执行动作**：走 `caisen-kline` 引擎即可，**四色层默认开启**（`render(four_color=True)` 为默认值，无需传参）。
**不得**再输出红绿二色 K 线图。

**四色语义（严格照抄 Pine 脚本，不改语义）**：`fast=ema(close,12)`、`slow=ema(close,50)`
- 红 `#d81e06`：close ≥ 快线 且 ≥ 慢线 → 多头强势区（连续红）
- 黄 `#e8a33d`：close < 快线 但 ≥ 慢线 → 多头回调
- 蓝 `#1565c0`：close ≥ 快线 但 < 慢线 → 空头反弹
- 绿 `#12a13f`：close < 快线 且 < 慢线 → 空头弱势区（连续绿）
- 色号相对 Pine 微调（黄/绿提饱和、EMA50 线改紫 `#7b1fa2`）**只为白底可读与避免与「空头反弹蓝」撞色，语义不变**。

**五个固定要素（缺一即不合规）**：
1. **实体填充管涨跌**：实心 = 收阴，空心（白底+同色描边）= 收阳 —— 四色管阵营、涨跌管方向，两维不互吞。
2. **量价层**：量柱与 K 线同色 + 量 MA5（灰虚线，放量/缩量基准）+ 区间均量（灰点线）。
3. **暖机区如实标注**：EMA50 需 116 根才稳定（EMA12 需 28 根）→ 前 116 根轻淡化 0.72 + 虚线分隔 + 图例说明；
   数据 <116 根时全图重淡化 + 红字【警讯】。**取数 wantNum 建议 300~400**；新合约取不到更多时保留淡化，不许抹掉警讯。
4. **侧切换三角**：多空阵营（R/Y ↔ B/G）翻转且**连续 ≥3 根确认**才画（▲下方 / ▼上方留白带，不压 K 线）。
5. **文字副图【六、四色K线节奏】**：现状 + 最近侧切换 + 量价读数（`x.xx×量MA5`、近5根量能±%、价±% → 量价四象限）。

**⚠️ 展示层纪律（硬约束）**：四色/量价**只作展示与一致性交叉校验**，**绝不参与 A/B/K 招判定**
（颈线、等幅满足、失败识别仍为纯形态几何）。它只提示「四色与形态方向背离」，**不自动改判方向**。

**回归**：`caisen-kline/test_caisen_trend.py` 45 项 + 原有 14 套回归全绿；`verify_events_layout.py` 几何自检
（要点框重叠 0 / 压 K 线 0 / 价位标签重叠 0）不受影响。

**成品样例（2026-09-20 实机）**：白糖2701（SR2701·郑商所·165 根不复权）→
`$CAISEN_ROOT/output/caisen-kline/SR2701_FUT_CZCE_caisen_4c.png`（4274×4082 / 1.59MB）+
同名 `.md`；识别为**假突破（翻空）**，颈线 5382.00，形态顶 5405.00，H=23，目标① 5359.00／目标② 5336.00，
现价 5335 已跌破颈线 −0.9%。

**待办（未实测）**：`tdx_lookup_stock(range="QH")` 现已能返回 **工业硅 SI / 多晶硅 PS（setcode=66，广期所）**，
而 `caisen_data.MARKETS["FUT_GFEX"]` 仍登记为 `supported=False`（未实测取数）。若后续实测 `tdx_kline(code="SI2609",
setcode="66")` 能返回非空 Rows，应把 GFEX 从「硬拒绝」改为「可用」并同步 SKILL.md / 本约定的支持范围。
