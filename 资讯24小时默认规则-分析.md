# 「拿数据默认 24 小时资讯」规则 — 可行性分析与落地方案

> 状态：已落地（2026-07-24 执行，默认 24h + 研究豁免 7d）
> 日期：2026-07-24
> 范围：蔡森 skill 工作区「拿数据 / 资讯」相关入口

---

## 0. 背景

用户提出：今后「拿数据」默认应该是 **24 小时内的资讯**。

经排查，工作区里「拿数据」的入口并不统一，默认时间窗口也各不相同。本文先把现状盘清，再判断 24h 默认是否合理、能否落地、落地成本与影响面，最后给出建议方案。

---

## 一、现状盘点（「拿数据」入口分三类）

### A 类：行情 / 财务 / K线 / 宏观（与「24h」无关，绝不能套）
- `data_quote` `data_kline` `data_minute` `data_finance` `data_macro` `data_technical` `data_chip` 等
- 本质是**历史序列 / 截面数据**，月线、年报、多年宏观都在此类。
- **结论**：24h 默认对这类无意义且有害，规则不应触及。

### B 类：资讯 / 新闻 / 公告 / 研报（westock-mcp，本次规则的主战场）
- `data_news`（新闻/公告/研报/全部，靠 `type` 区分）
- `data_notice`（公告）
- `data_report`（研报）
- `data_dehydrated`（脱水研报）
- **现状**：参数只有 `mode / symbol / id / limit`，**腾讯服务端无时间窗口入参**。默认 `limit=20`，返回按时间倒序，但调大 limit 会混入更早的条目，无法靠参数锁 24h。
- **实测（2026-07-24 对 sh600519 调 data_news limit=5）**：返回带 `time` 字段（格式 `2026-07-24 19:50:51`），且已倒序。→ **取回后可本地按 `time` 筛 24h，完全可行。**

### C 类：舆情聚合（last30days / news-aggregator，定位不同，建议不改）
- `last30days` 默认近 30 天多平台舆情，是独立的 WebSearch/scrape 引擎，时间窗写死在脚本里，可配。
- 它服务于「话题舆情脉络」，与「个股/市场当日资讯」是两回事。**建议不纳入本次 24h 规则**，除非用户另要舆情收窄。

---

## 二、合理性判断（不该一刀切）

✅ **适用 24h 默认的场景**
- 盘前 / 每日简报 / 日内交易决策（过时新闻对盘中噪声大）
- 资本三流日报、资讯快讯类输出

❌ **不应强制 24h 的场景**
- 深度研究 / 事件复盘 / 成因分析（如蔡森形态、破底翻、资金情绪框架，常需看一周乃至更长的催化脉络）
- 单票重大事件回溯

**核心结论**：24h 默认应**只作用于 B 类资讯接口 + 当日简报类场景**，并对研究/复盘场景留「显式豁免」（可指定 7d / 30d）。

---

## 三、落地方案（推荐）

### 3.1 唯一改动点：`westock-data` 技能规范
在 `stock-partner-team/skills/westock-data/SKILL.md` 新增一节 **「资讯时效过滤（默认 24h）」**：

> **资讯时效过滤规范**
> 1. 调用 `data_news` / `data_notice` / `data_report` / `data_dehydrated` 后，**必须按返回 `time` 字段本地过滤**，仅保留 `now − 24h` 内的条目后再展示/进分析。
> 2. 默认 `limit` 取 **30**（覆盖 24h 余量，又不过度拉取）。
> 3. 过滤解析：`time` 格式 `%Y-%m-%d %H:%M:%S`，与当前时间比较，丢弃超 24h 的。
> 4. **场景豁免**：深度研究 / 事件复盘 / 成因分析时，可显式放宽窗口（如 `7d` / `30d`），不强制 24h；此类需求应在调用时说明。
> 5. 腾讯接口**不支持时间参数**，此过滤是「取回后」行为，不是传参。

### 3.2 其他技能不必各自重写
`caisen-10-experts-analyst` 等圆桌技能调用 westock 取资讯时，**引用本规范即可**（「资讯时效遵循 westock-data 的 24h 默认，研究场景可申请豁免」），避免重复定义、日后两处漂移。

### 3.3 过滤伪代码（供技能内实现参考）
```python
from datetime import datetime, timedelta

def filter_24h(items, now=None):
    now = now or datetime.now()
    cutoff = now - timedelta(hours=24)
    out = []
    for it in items:
        t = datetime.strptime(it["time"], "%Y-%m-%d %H:%M:%S")
        if t >= cutoff:
            out.append(it)
    return out
```

---

## 四、影响面与风险

| 项 | 影响 |
|----|------|
| 行情/财务/K线/宏观 | 不受影响（规则不触及 A 类） |
| 个股/市场资讯输出 | 默认更「鲜」，旧闻自动剔除 |
| 深度研究类分析 | 需显式豁免，否则可能漏掉一周前的催化 |
| last30days 舆情 | 不改（定位不同） |
| 实现成本 | 极低（仅改 SKILL.md 规范 + 调用时加一步本地筛） |

**主要风险**：若圆桌技能忘记「研究场景豁免」，可能在做形态/资金分析时因资讯被截到 24h 而丢失背景。对策：规范里把豁免写死为「默认 24h，研究明确放宽」。

---

## 五、结论与待确认

**分析结论**：
1. 「默认 24 小时资讯」**技术上完全可行**——westock 资讯接口虽无时间参数，但返回带 `time`，取回后本地筛即可。
2. 规则应**限定在 B 类资讯接口 + 当日简报场景**，不波及行情/财务/宏观，且对研究/复盘留显式豁免。
3. 落地成本极低，只改 `westock-data` 一处规范，其他技能引用即可。

**落地确认（2026-07-24，用户回复「开始」）**：
- [x] 按方案落地：已改 `westock-data` SKILL + 加本地筛规范（含 `filter_recent` 参考实现）
- [x] 范围限定 B 类资讯接口（`data_news`/`data_notice`/`data_report`/`data_dehydrated`）；C 类舆情（last30days）不动
- [x] 研究/复盘豁免默认窗口 = **7d**（可指定 30d）

**实际改动文件**：
1. 主副本 `stock-partner-team/skills/westock-data/SKILL.md`
   - 新增「资讯时效过滤（默认 24h）」章节（铁律 + 适用接口 + 4 条规范 + Python 过滤实现）
   - 「新闻与研究」小节代码块后加脚注，引用该章节
2. 镜像 `~/.workbuddy/skills/stock-partner-team/skills/westock-data/SKILL.md`（已 cp 同步，grep 校验 2 处命中）

**生效方式**：腾讯接口无时间参数，规则靠「取回后按 `time` 字段本地筛 24h」实现；深度研究/复盘场景显式豁免至 7d。
